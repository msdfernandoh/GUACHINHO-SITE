import Link from "next/link";
import { notFound } from "next/navigation";
import { MODELOS_PROGRAMA_PARCEIROS } from "@/lib/parceiros/modelos-programa";
import { InstalarAppParceiroButton } from "@/components/public/instalar-app-parceiro-button";
export default async function ModeloParceiroPage({ params }: { params: Promise<{modelo:string}> }) { const {modelo}=await params; const item=MODELOS_PROGRAMA_PARCEIROS.find((m)=>m.slug===modelo); if(!item) notFound(); return <main className="min-h-screen bg-zinc-950 px-5 py-16 text-white"><section className="mx-auto max-w-3xl"><Link href="/parceiros" className="text-sm font-bold text-amber-400">← Todos os modelos</Link><p className="mt-10 font-bold text-amber-400">PROGRAMA DE PARCEIROS</p><h1 className="mt-3 text-5xl font-black">{item.nome}</h1><p className="mt-4 text-3xl font-black text-amber-400">Participação comunicada: {item.ganho}</p><p className="mt-3 text-lg text-zinc-300">{item.resumo}</p><div className="mt-10 grid gap-4 sm:grid-cols-3">{item.vantagens.map((v)=><div key={v} className="rounded-2xl border border-zinc-700 p-5 text-zinc-200">{v}</div>)}</div><p className="mt-8 text-sm text-zinc-400">{item.meta}. Valores estimativos e sujeitos às regras comerciais vigentes e ao recebimento efetivo.</p><div className="mt-10 flex flex-wrap items-center gap-4"><Link href={`/parceiros/cadastro?modelo=${item.id}`} className="inline-block rounded-2xl bg-amber-500 px-7 py-4 font-black text-zinc-950">Quero seguir com este modelo →</Link><InstalarAppParceiroButton dark /></div></section></main>; }
